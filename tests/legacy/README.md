# Historical configuration UI acceptance scripts

These STEP 9–10 scripts describe the retired workbench and must not be used as current route acceptance tests. Retained as historical test evidence, not executed by lint/build or the current browser suite. Current route coverage: `tests/site-reset.browser.cjs`. Core `configuration.test.mjs` still protects the retained engine and historical cart snapshots.

The site-reset browser script also describes the superseded Hero/seven-theme system. Current architecture acceptance is tests/product-v2.browser.cjs.
